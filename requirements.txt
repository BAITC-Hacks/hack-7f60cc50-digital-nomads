# Пайплайн
pandas>=2.0
pyarrow>=14.0        # чтение .parquet
numpy>=1.24
networkx>=3.1
scipy>=1.10          # нужен networkx для PageRank
scikit-learn>=1.3    # модель обрыва 4-го колена (логистическая регрессия)

# viewer.html и serve.py дополнительных зависимостей не требуют:
# cytoscape.js лежит в vendor/, сервер написан на стандартной библиотеке Python.
